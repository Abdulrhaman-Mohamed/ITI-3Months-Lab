
// used an config class to put a settings for my object here if i want change in things of behiover of my object i will get here
export default {
    settings:{
        gridSelector:"#grid",
        borderSize:1,
        cellSize:60,
        borderColor:'gray'
    }
}